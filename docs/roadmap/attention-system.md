# Attention system

> Status: candidate — not yet discussed in detail. Delight track.

## Idea

OS-level notifications + unread tab badges when an agent turn finishes
or errors — so background worktrees actually get noticed.

## Notes

- `busyChats` / `activeWork` signals and the completion sound already
  exist; this is the Notification API + document-title badge + an
  unread marker on chat tabs.
- Quiet hours / per-chat mute would come later.

## Effort

S.
