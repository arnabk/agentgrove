# AgentGrove — Handoff (2026-08-13)

This handoff covers the current state of the `main` branch as of **2026-08-13**. It is intended for the next agent (or the next opencode session) so work can resume without reconstructing context.

## 1. Project basics

**AgentGrove** — local developer workspace. Rust backend + SolidJS/TanStack-Solid frontend. Cross-platform (Linux/macOS/Windows).

- Repo: `https://github.com/arnabk/agentgrove`
- Branch: `main`
- Current version: `0.1.55` (`Cargo.toml`)
- Rust toolchain: `1.95.0` (pinned via `rust-toolchain.toml`)
- Dev: `just dev` → BE on `:4317`, FE on `:5173`
- Health: `curl -fsS http://127.0.0.1:4317/health`
- Important rule: **do not commit `Cargo.lock`** — discard it before push.

## 2. Recent work (since the last handoff snapshot)

- **Client memory/OOM guards** — capped chat window (600 prompts / 400 events per prompt) and terminal scrollback (2000 lines), throttled memory accounting.
- **GalaxyMap leak fix** — removed a leaked window resize listener.
- **Stale opencode session recovery** — when the native opencode store no longer knows the session, the backend clears `session_id`, retries with recent context, and keeps the chat alive.
- **`/compact` chat command** — summarizes the conversation and starts a fresh provider session in place.
- **Demo recorder fixes** — Postgres seed baked into `docker/Dockerfile.postgres`; demo compose uses service names instead of host ports.
- **Combined demo video** — `docs/demos/agentgrove-demo.mp4` (5:49, H.264/AAC), built by concatenating existing per-feature clips recorded inside the isolated Docker stack.
- **README rewrite** — less AI-generated copy, expanded feature list, single demo thumbnail link.
- **Releases** — `v0.1.53`, `v0.1.54`, and `v0.1.55` shipped automatically on push to `main`. All three README badges (CI, Release, Nightly) are green.
- **Announcement threads** — dev.to and Medium comments bumped to `v0.1.53`. HN posting is rate-limited; Reddit is blocked by network security, so both are excluded from the launch channel list going forward.

## 3. Current state

- **Latest commit:** `f459e78` (README rewrite + handoff checkin).
- **CI:** green for `ci.yml`, `release.yml`, and `nightly.yml`.
- **Latest release:** `v0.1.55`.
- **Untracked:** `.serena/` (Serena tooling state).
- **Known long-running threads:**
  - 18h renderer OOM — mitigated by bounded retention; root cause (possible WS reconnect accumulation) not yet fully resolved. Next step is to read `.data/logs/client.log` after a long run and look for `sustainedClimb` warnings and `ws:` counts.
  - Manual queue mode — user wants queued messages to stay until explicitly picked. Not implemented.
  - Terminal zsh-autocomplete ghosting — reported but not fully resolved.

## 4. Operational notes

- Start the dev server with `just dev` if it is not already up.
- Before every commit/push: `just verify` (or at least `just check`), and verify all three README badges are green.
- Record demos in the isolated Docker stack (`docker/docker-compose.demo.yml`) on port `4320`.
- Post release updates to dev.to and Medium; skip HN and Reddit.
- Data-safety: see `docs/operations/data-safety.md`. Always test chat visibility after worktree delete/restore changes.

## 5. Roadmap (approved items in `docs/roadmap/README.md`)

1. GitHub issues board
2. Review loop — inline diff comment → follow-up prompt
3. Native PR flow
4. Composer `@`-mention file autocomplete
5. Scheduled automations UI
6. PR management view
7. Agent-runs inbox

Nothing may be added to this list without explicit user approval.

---

_Generated 2026-08-13. Previous thread: `ses_0e5aa8ebdffeue27u2zegD56KI` (2026-06-30 → 2026-07-28)._
